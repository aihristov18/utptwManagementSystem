#include <iostream>
#include <conio.h>
#include "Menu.h"
using namespace std;

int main() try
{
   bool isRunning = true;
    if (isRunning)
    {
        isRunning = runtime();
    }
}
catch (exception& err)
{
    cerr << err.what() << endl;
}