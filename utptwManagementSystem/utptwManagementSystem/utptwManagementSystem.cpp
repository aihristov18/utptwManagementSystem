#include <iostream>
#include <conio.h>
#include "Menu.h"
using namespace std;

int main() /*try*/
{
   bool isRunning = true;
    if (isRunning)
    {
        isRunning = runtime();
    }
}
//catch (std::exception& e)
//{
//    std::cerr << e.what() << std::endl;
//    return EXIT_FAILURE;
//}